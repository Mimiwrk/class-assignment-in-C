    //control column
    int row = 1;
    while( row <= size ) {
        //function that print row
        if(size != 0){
            printChars(row, symbol);
        }
        else{
            printChars(row, symbol2);
        }
    row++;
    }